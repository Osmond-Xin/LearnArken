# Deep Research 每日提示词库（Day 1–13）

> **AI-drafted**（Claude），2026-07-15，配合学习流程 v2 第 1a 步（研）。
> 用法：开工当天复制当日提示词 → 粘贴到 **Gemini 客户端的 Deep Research**
> → 等待报告 → 下载后存为 `docs/research/dayN-report.md`，文件开头加一行：
> `> 来源：Gemini App Deep Research，日期 YYYY-MM-DD。`
> 存档后告知 Claude，由其执行第 1c 步（未知点扫描 → `dayN-unknowns.md`）。
>
> 每条提示词自包含（Gemini 看不到本仓库），已内置读者画像与项目背景。
> Day 1–3 的提示词用于回溯补课，可选。

---

## Day 1 — S1000D 标准与航空技术出版物

```text
你是航空技术出版物领域的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，正在做一个"面向 S1000D 风格航空技术出版物的智能化平台"学习项目（用自造合成 XML 模拟，不接触真实受控数据）。

调研领域：S1000D 标准体系与技术出版物数据工程。

报告必须包含：
1. 背景：技术出版物行业要解决什么问题，为什么飞机维修手册不能是普通 Word/PDF；
2. 发展源流：从 ATA 100 → ATA iSpec 2200 → S1000D 各 Issue 的演进脉络与关键年份，与 DITA 的关系和分野；
3. 核心概念详解：数据模块（DM）、DMC 编码结构、DML、出版物模块（PM）、BREX 业务规则、SNS、applicability（适用性）、CSDB——每个概念给一句通俗解释和一个传统软件工程类比（如 DMC ~ 数据库主键设计）；
4. 当前主流：2025–2026 年主流 S1000D 工具链与厂商格局，开源工具（如 s1kd-tools）现状；
5. 未来方向：S1000D 与 AI/结构化数据智能化的结合趋势；
6. 必须掌握的技巧与常见误区：新手理解 S1000D 时最容易犯的 5 个错误；
7. 面试高频问题：若面试"航空数字化/技术出版物智能化"岗位，5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释。
```

## Day 2 — XML 校验与数据质量工程

```text
你是 XML 数据工程与数据质量领域的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，正在为 S1000D 风格的航空 XML 文档实现一个四层校验器（良构性 → schema → 业务规则 → 跨文件完整性）。

调研领域：XML 校验技术栈与数据质量工程。

报告必须包含：
1. 背景：为什么受控行业（航空/医疗）的文档校验是分层的，"fail-closed（失败即拒绝）"原则的来历；
2. 发展源流：DTD → XSD → RelaxNG → Schematron 的演进，各自解决什么、放弃什么；
3. 核心概念详解：XSD 结构校验 vs Schematron 断言式校验的本质区别；S1000D 的 BREX 机制如何用 Schematron 思想做业务规则；跨文件引用完整性（悬空引用、循环引用）与引用图；每个概念配传统软件工程类比（如 Schematron ~ 数据库 CHECK 约束 + 触发器）；
4. XML 安全：XXE 注入、实体炸弹（billion laughs）、DTD 攻击的原理与防御（defusedxml/lxml 加固），2025–2026 年的最佳实践；
5. 当前主流与未来：校验工具链格局，规则即代码（rules-as-code）与 AI 辅助校验的趋势；
6. 必须掌握的技巧与坑：实现多层校验器时的 5 个常见错误（如层间失败传播、行号定位、报告可操作性）；
7. 面试高频问题：数据质量/校验管线方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释。
```

## Day 3 — 经典信息检索：BM25 与检索评估

```text
你是信息检索（IR）领域的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，正在对 S1000D 风格航空文档（含大量零件号/DMC 标识符）实现 BM25 基线检索和评估体系。

调研领域：经典（词法）信息检索与检索质量评估方法学。

报告必须包含：
1. 背景：搜索引擎与数据库查询的本质区别，相关性排序要解决什么问题；
2. 发展源流：布尔检索 → TF-IDF → BM25（1994）→ 学习排序（LTR）的演进与关键论文；
3. 核心概念详解：倒排索引（类比数据库索引）、BM25 的 k1/b 参数直觉、分词器/analyzer 对技术语料的决定性影响（标识符被标点切碎的经典失败模式）；
4. 评估方法学（重点）：golden set（相关性标注集）怎么建、为什么相关性判断必须人做、标注粒度选择；Recall@k、MRR、nDCG 各自的适用阶段与使用惯例；无答案问题（no-answer trap）的评估意义；
5. 评估陷阱：训练/评估泄漏、样本量不足、种子不固定、指标挑拣（cherry-picking）——红队攻击评估方法时会查什么；
6. 当前主流与未来：2025–2026 年 BM25 在 RAG 时代的定位（为什么没死）、Tantivy/Lucene 生态；
7. 面试高频问题：检索评估方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比。
```

## Day 4 — 向量检索与混合检索（embedding / ANN / RRF / 重排）

```text
你是资深信息检索系统专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，今天要动手实现「MiniMax embedding + Vespa 稠密检索 + BM25/RRF 混合检索 + 交叉编码器重排」的消融实验（语料为 S1000D 风格航空技术出版物，含大量零件号/DMC 标识符）。

调研领域：文本 Embedding 与向量检索、ANN 索引（HNSW/IVF/PQ）、混合检索与 RRF 融合、交叉编码器重排、向量数据库（重点 Vespa，对比 Qdrant/pgvector/Milvus 格局）。

报告必须包含：
1. 背景与要解决的问题（词汇失配 vs 精确匹配的根本矛盾）；
2. 发展源流：TF-IDF/BM25 → 词向量 → DPR → ColBERT（late interaction）→ 混合检索的演进脉络、关键论文与年份；
3. 当前主流：2025–2026 年 embedding 模型格局（含中文/多语模型与 MiniMax 的定位）、向量数据库格局、MTEB 榜单现状与其局限；
4. 未来方向（MRL 套娃向量、agentic retrieval 等）；
5. 最佳实践：消融实验设计、Recall@k/MRR/nDCG 的阶段分工、golden set 标注；
6. 必须掌握的技巧与坑：embedding 归一化与距离度量一致性、query/doc 不对称编码、标识符查询 dense 退化、RRF 的 k 参数、rerank 截断深度、延迟预算；
7. 针对本场景（小语料 + 标识符密集 + Vespa）的具体建议清单，含 Vespa schema/rank-profile（first-phase / global-phase）要点；
8. 面试高频问题：混合检索方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实（模型名、版本、Vespa 能力）并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比。
```

## Day 5 — RAG 与带引用的可信问答

```text
你是 RAG（检索增强生成）系统的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，今天要在已有混合检索之上实现"带引用的问答"：LLM 生成答案、每个论断标注出处 chunk、证据不足时明确拒答（fail-closed）、全链路 trace 落盘（语料为 S1000D 风格航空维修文档——错误答案有安全后果）。

调研领域：RAG 架构、引用与 groundedness、拒答机制、LLM 应用基础。

报告必须包含：
1. 背景：为什么"让 LLM 直接回答"在受控领域不可接受，幻觉的成因（简要讲 LLM 的下一词预测本质、上下文窗口、温度采样）；
2. 发展源流：RAG 概念的提出（2020）→ 朴素 RAG → 高级 RAG →模块化/agentic RAG 的演进；
3. 核心技术详解：结构化 prompt 设计（系统指令/证据区/引用格式约定）、citation 的实现模式（chunk id 标注、span 对齐）、拒答逻辑的实现（证据充分性判断）、answer trace 审计（query→检索→prompt→答案→引用全链路留痕）；
4. 评估方法（重点）：citation coverage、groundedness/faithfulness 的定义与测法，人工抽查怎么设计，自动评估与人工的关系；
5. 当前主流与未来：2025–2026 年主流 RAG 框架与"无框架手写"之争、Claude API 的结构化输出/citations 能力现状；
6. 必须掌握的技巧与坑：引用漂移（引了不支撑论断的块）、拒答阈值过松/过紧、prompt 注入（检索内容里藏指令）、长上下文塞满 vs 精选证据；
7. 面试高频问题：RAG 方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比（如 trace ~ 分布式链路追踪）。
```

## Day 6 — LLM 应用的服务化：API 与 Demo 工程

```text
你是 AI 应用工程化的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，今天要把已有的"文档校验 + RAG 问答"能力封装为 FastAPI 服务（POST /validate、POST /query）并做一个 Streamlit 本地 demo，一条命令起全套。

调研领域：LLM 应用的 API 服务化、demo 工程与可观测性。

报告必须包含：
1. 背景：AI 能力从 CLI/脚本到服务化要跨过哪些坎（状态、并发、超时、密钥）；
2. 技术详解：FastAPI + Pydantic 的契约式 API 设计（OpenAPI 自动文档）、LLM 调用的超时/重试/流式响应（SSE）处理、长任务的同步 vs 后台模式；
3. Demo 工程：Streamlit 的定位与限制（vs Gradio），"评审者 10 分钟内跑通"的 demo 设计原则，make 一键启动的组织方式；
4. 可观测性（重点）：结构化日志、request id 贯穿、LLM 调用的 token/延迟/成本记录、audit trail 在受控行业的意义；
5. 安全与配置：API 密钥管理（.env、绝不入库）、输入校验、速率限制；
6. 当前主流与未来：2025–2026 年 LLM 服务化技术栈格局（FastAPI 地位、LiteLLM 等中间层）、demo 托管选项；
7. 必须掌握的技巧与坑：5 个常见错误（如密钥泄漏进日志、无超时导致悬挂、demo 与 API 逻辑重复实现漂移）；
8. 面试高频问题：AI 应用工程化方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释。
```

## Day 7 — AI Agent 与自动修复闭环

```text
你是 AI Agent 系统的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，今天要实现一个"校验修复 agent"：读取 XML 校验器的 findings，结合检索到的规范上下文，生成修复 patch（默认 dry-run，显式 --apply 才落盘），修复后自动重跑校验器闭环验证，每个 patch 附理由与引用。

调研领域：AI Agent 架构与自动修复（self-healing / auto-remediation）系统。

报告必须包含：
1. 背景：agent 与普通 LLM 调用的本质区别（循环、工具、状态），"生成修复建议"与"自动改数据"之间的责任鸿沟；
2. 发展源流：ReAct（2022）→ 工具调用/function calling → 规划-执行架构 → 2025–2026 年的 agent 工程共识（如 Anthropic 的 building effective agents 思想：能用 workflow 就别用 agent）；
3. 核心技术详解：工具接口设计、dry-run/apply 双模式的安全价值、闭环验证（修复后复跑校验器）为什么是 agent 可信的关键、patch 附理由与引用的可审计性；
4. 评估方法（重点）：修复成功率怎么定义（修好目标违规 + 不引入新违规）、失败案例分析的写法；
5. 失败模式：无限修复循环、过度修复（改了不该改的）、投毒上下文导致错误修复、验证器与修复器共谋；
6. 当前主流与未来：代码修复 agent（SWE-bench 类）与数据修复 agent 的现状格局；
7. 必须掌握的技巧与坑：5 个实现要点（如幂等性、修复顺序依赖、单文件失败不污染整包）；
8. 面试高频问题：agent 方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比（如 dry-run ~ 数据库事务先 EXPLAIN 后 COMMIT）。
```

## Day 8 — 对抗评估与 LLM-as-Judge

```text
你是 AI 系统评估与红队方法论的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，今天要"攻击自己的 RAG 系统"：构造对抗评估集（同义改写、跨模块陷阱、无答案问题、零件号扰动，≥30 例），用 LLM-as-judge 自动评 groundedness，并与此前的人工抽查对齐校准，修复暴露的缺陷并报告前后指标。

调研领域：对抗评估（adversarial evaluation）、LLM-as-judge、AI 红队方法论。

报告必须包含：
1. 背景：为什么"平均指标好看"不等于系统可靠，对抗评估与常规评估的关系；
2. 发展源流：软件测试的 fuzzing/属性测试传统 → NLP 对抗样本 → LLM 红队 → 2025–2026 年 AI 评估工程的现状；注意区分 adversarial validation 在 governed-AI 语境（对抗性验证工作流）与传统 ML 语境（分布偏移检测）的不同含义；
3. 对抗集设计方法（重点）：改写不变性（答案不该变）、扰动敏感性（零件号变了答案必须变或拒答）、无答案陷阱、跨文档混淆——每类的构造技巧与数量配比；
4. LLM-as-judge 详解：评分 prompt 设计、已知偏差（位置偏差、长度偏差、自恋偏差/self-preference）、为什么必须报告 judge 与人工的一致率而不许只报 judge 数字、校准方法（Cohen's kappa 等）；
5. 证据链写法：发现缺陷 → 修复 → 指标前后对比的报告规范；
6. 当前主流与未来：评估工具生态、judge 模型选择（与被评系统不同源的原则）；
7. 必须掌握的技巧与坑：5 个常见错误（如对抗集泄漏进修复、judge 与生成用同一模型、只修指标不修根因）；
8. 面试高频问题：AI 评估方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释。
```

## Day 9 — 面向 AI Agent 的机器可读文档与证据链

```text
你是开发者体验（DX）与 AI 时代技术文档的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，今天要把自己的项目仓库改造成"招聘方及其 AI agent 可核查"的形态：llms.txt 仓库导览、EVIDENCE.md（每条对外声明 → 仓库内证据的映射表）、AI 协作工作流说明文档。

调研领域：机器可读文档标准、可核查仓库设计、AI-first 工作流的对外呈现。

报告必须包含：
1. 背景：招聘方与技术评审开始用 AI agent 读仓库意味着什么，"人读 README、机器读什么"的问题；
2. llms.txt 标准详解：起源、规范、2025–2026 年采纳现状、与 robots.txt/sitemap 的类比、写法最佳实践与反模式；
3. 证据链设计（重点）：可复现性声明的工程化（每个数字 → 复跑命令 → 数据集版本）、evidence mapping 的组织方式、供应链信任（commit 溯源、Co-Authored-By 的诚实标注实践）；
4. AI 协作工作流的对外表述：如何向雇主证明"人写规格与判断、AI 写实现"的分工是真实的（不可伪造的人类产出：决策记录、裁决、日志）；adversarial validation 一词在 governed-AI 行业的用法及与 ML 传统含义（分布偏移检测）的区别；
5. 当前主流与未来：AI 可读文档的生态走向（agents.md 等相关约定）、作品集仓库被 AI 评估的趋势；
6. 必须掌握的技巧与坑：5 个常见错误（如 llms.txt 写成营销文案、证据链断链、声明与证据数字不一致）；
7. 面试高频问题：围绕"AI-first 工作流与可核查性"的 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实（llms.txt 现状、相关标准）并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释。
```

## Day 10 — 部署上线与技术作品集呈现

```text
你是 AI 应用部署与开发者作品集的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，今天要把一个"S1000D 文档校验 + 混合检索 + 带引用 RAG"的学习项目上线为在线 demo（候选：Streamlit Community Cloud、HuggingFace Spaces），并把 README 定稿为面向招聘方的作品集门面。

调研领域：轻量级 AI demo 部署、README/作品集工程。

报告必须包含：
1. 背景：在线 demo 对求职作品集的价值与边界（评审者最多给你几分钟）；
2. 部署选型详解：Streamlit Community Cloud vs HuggingFace Spaces vs 其他轻量选项的 2025–2026 年现状对比（免费额度、冷启动、密钥管理、资源限制、休眠策略）、依赖外部 API（付费 LLM key）的 demo 怎么控制成本与滥用（速率限制、演示模式、预生成缓存）；
3. Secrets 与安全：部署平台的密钥注入机制、绝不把 key 编译进前端、演示环境的降级策略（key 失效时 demo 仍可看）；
4. README 工程（重点）：招聘方视角的 README 结构（30 秒电梯段、demo GIF、基准表、Quickstart 三条命令、诚实分层 Roadmap）、demo GIF 的制作工具与规范、badge 的克制使用；
5. 口头呈现：60 秒项目陈述的结构（问题 → 方案 → 数字 → 我的独特贡献），技术面试中演示 demo 的节奏控制；
6. 必须掌握的技巧与坑：5 个常见错误（如 demo 首屏加载超 30 秒、README 数字与仓库证据对不上、依赖本人电脑的"在线"demo）；
7. 面试高频问题：围绕项目呈现的 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实（各平台 2026 年免费政策）并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释。
```

## Day 11 — 知识图谱增强检索（KG-RAG / GraphRAG）

```text
你是知识图谱与 RAG 结合方向的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，已有一个 S1000D 风格航空文档的混合检索 RAG 系统（BM25 + 稠密向量 + RRF 融合 + 重排，带消融评估），文档间引用关系已抽成一张确定性依赖图（XML 显式引用映射，非 LLM 抽取）。今天要把这张图升级为检索路径的第三路信号：query 实体链接（DMC/零件号，正则优先）→ 沿引用/依赖边取 1–2 跳邻域 → 邻居 chunk 进 RRF 融合，并在消融表加一行 hybrid+graph。

调研领域：知识图谱增强检索（KG-RAG）、GraphRAG 及相关路线。

报告必须包含：
1. 背景：纯文本检索在多跳/组合问题（答案分散在引用链多个文档上）上的失败模式，为什么"关系"是文本相似度捕捉不到的信号；
2. 发展源流与术语辨析（重点）：KG-RAG / GraphRAG / graph-augmented retrieval / HybridRAG 各指什么，微软 GraphRAG（2024，实体抽取+社区检测+社区摘要）的定位、成本与适用边界，与"确定性结构图 + 邻域扩展"轻量路线的对比；
3. 核心技术详解：实体链接（技术标识符的确定性规则 vs NER vs LLM 解析，置信度与 fail-closed 校验）、邻域扩展设计（跳数、边类型白名单、hub 节点度数截断）、扩展结果的两种用法（候选扩展进融合 vs 已有候选加权）、与 RRF 多路融合的整合方式；
4. 评估方法（重点）：怎么设计消融实验证明 graph 路的真实贡献（单变量对照、多跳 golden 题的构造与新旧题分开报分、no-answer 陷阱在图扩展下的失守风险）、graph 路对延迟的影响怎么报；
5. 失败模式：邻居噪声淹没直接命中、hub 爆炸、实体链接错误级联、图与索引不一致——每个给检测手段与修法；
6. 当前主流与未来：2025–2026 年 KG-RAG 工具生态（Neo4j GraphRAG、LlamaIndex/LangChain 图检索模块等）与 agentic retrieval 趋势下图谱的角色；
7. 必须掌握的技巧与坑：实现这类系统的 5 个常见错误；
8. 面试高频问题：KG-RAG 方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实（GraphRAG 论文与工具现状）并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比（如邻域扩展 ~ 数据库外键 join 扩展查询）。
```

## Day 12 — 多模态 RAG 与视觉文档理解

```text
你是多模态 AI 与文档智能方向的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，已有一个 S1000D 风格航空文档的 RAG 系统（带引用与拒答）。今天要让技术插图（S1000D 的 ICN：分解图、hotspot 编号标注）进入检索与问答：入库时用视觉语言模型（VLM）对每张图生成 schema 约束的结构化描述（部件清单、hotspot 编号、警告），描述进文本索引（describe-then-index），回答可引用图（图 id + 描述出处），描述覆盖不了的问题拒答；插图为自绘合成 SVG（不接触真实图纸）。

调研领域：多模态 RAG、视觉语言模型（VLM）应用、视觉文档检索、多模态编排（multi-modal orchestration）。

报告必须包含：
1. 背景：技术文档中"信息只在图里"的普遍性，纯文本 RAG 的盲区；
2. 路线版图（重点）：describe-then-index（VLM 描述进文本索引）vs 共空间多模态 embedding（CLIP/SigLIP 系，对密集线框技术图的退化问题）vs 视觉文档检索 late-interaction 路线（ColPali/ColQwen，2024 起：整页当图、patch 级 MaxSim、免解析管线）——各自原理、成本、引用粒度、适用场景与 2025–2026 年现状；
3. 核心技术详解：VLM 的图像 token 化机制与上下文成本、结构化 captioning（schema 约束输出、封闭候选词表压幻觉）、描述与图文件 checksum 绑定防漂移、图引用的可审计设计、超出描述范围问题的 fail-closed 拒答；
4. 多模态编排（重点）：入库期重活 vs 查询期轻载的成本分账、什么问题触发"二次看图"（取原图再调 VLM 复核）、预算护栏（限次/限分辨率/trace）、主流 VLM（GPT-4V/o 系、Claude、Qwen-VL、Gemini）在文档理解上的格局；
5. 评估方法：看图问题 golden set 设计（答案在图/在文/图文冲突陷阱）、图引用正确率、对原文本指标的回归检查；
6. 失败模式：VLM 细节幻觉（编号/箭头/小字）、描述漂移、图 chunk 挤占正文召回、陷阱题失守——每个给检测与修法；
7. 必须掌握的技巧与坑：5 个常见错误（如批量多图串图、分辨率与 token 成本失控）；
8. 面试高频问题：多模态 RAG 方向 5 个高频问题及答题要点。

要求：用 web 搜索核实关键事实（ColPali 论文与后续、各 VLM 文档理解能力现状）并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比（如 describe-then-index ~ 附件 OCR 进全文索引）。
```

## Day 13 — Python 性能工程：并发版图与热点优化

```text
你是 Python 性能工程与并发编程的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身（Java 时代经历过锁/竞态/死锁的维护成本，此后一直用架构规避共享可变状态——队列、无状态服务、幂等写入），正转型 AI 工程，项目基线 Python 3.12。今天要在一个已有的 RAG 系统（XML 校验 + BM25/稠密混合检索 + 批量评估管线）里做性能工程切片：批量外部 API 调用改 asyncio（Semaphore 限并发 + 超时/重试），多包校验改 multiprocessing 分片并行并测 1/2/4/8 worker 扩展曲线，用 py-spy/cProfile 定位一个数值热点做「纯 Python / numpy / numba」三列对比，Rust/PyO3 仅在 profile 证明 Python 侧是瓶颈时才立项。

调研领域：Python 并发模型版图、asyncio、multiprocessing、编译加速（numba/Cython）、原生扩展（Rust/PyO3）、性能剖析方法学。

报告必须包含：
1. 背景：GIL 的原理与工程后果（线程为何对 CPU 任务零加速、对 I/O 为何仍有效），"share-nothing + 消息传递"在并发学界的地位——为什么"用架构消灭共享状态"是正统而非规避；
2. 2025–2026 年 GIL 现状（重点核实）：PEP 703 free-threaded 构建在 3.13/3.14 的支持阶段（PEP 779）、独立构建与 wheel 生态（cp313t/cp314t）兼容现状、PEP 734 子解释器、无 GIL 时代锁与竞态风险回归的讨论；
3. asyncio 详解：事件循环模型（与 nginx epoll 类比）、await 点交错的逻辑竞态（check-then-act 跨 await）、结构化并发（TaskGroup、asyncio.timeout，3.11+）、Semaphore 限流/超时/重试模式、事件循环内阻塞调用的危害与检测；
4. multiprocessing 详解：进程隔离与 pickle 序列化成本模型、ProcessPoolExecutor vs Pool、任务粒度与 chunksize、"传分片描述而非数据"的实践、扩展曲线拐点分析（Amdahl + 序列化 + 启动开销）；
5. 剖析方法学（重点）：py-spy（采样、火焰图、--native）与 cProfile 的分工、"先测量后优化"纪律、优化收益报告的规范（必须带数据规模/缓存状态等分母）；
6. 编译加速选型：numpy 向量化 → numba（@njit、nopython 静默回退坑、编译预热）→ Cython（何时值得上）的顺序纪律，各自适用面与典型收益区间；
7. Rust/PyO3 路线：PyO3 + maturin 工作流、GIL 释放（allow_threads）、"知情消费者"姿态（Tantivy/Qdrant 等 Rust 实现库）与自写扩展的决策边界；
8. 必须掌握的技巧与坑：5 个常见错误（如小任务负加速、孤儿任务吞异常、numba 静默回退、无分母加速比）；
9. 面试高频问题："你怎么处理并发"类 5 个高频问题及答题要点（含如何把"规避共享状态"的经历讲成判断力）。

要求：用 web 搜索核实关键事实（free-threading 各版本状态、PEP 编号与阶段）并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比（如 asyncio ~ nginx 事件循环、multiprocessing ~ share-nothing 微服务）。
```

## Day 13 附加 — 推理期搜索：ToT/GoT/MCTS、世界模型与自我质检

```text
你是 LLM 推理期搜索与 agent 架构的资深专家。请用中文为我做一份深度调研报告。读者画像：资深产品经理、传统程序员出身，正转型 AI 工程，已实现一个 ReAct 修复 agent（读取 XML 校验器 findings → 生成修复 patch → dry-run 沙箱执行 → 复跑校验器闭环验证）和一套对抗评估体系（攻击自己的 RAG、双异构 LLM 裁判 + Cohen κ 人工校准）。今天要做一个半日玩具实验：对同一违规生成 3 个候选 patch，各自沙箱复验打分取最优，对比单候选的修复成功率与 token 成本。

调研领域：推理期搜索（inference-time search）方法谱系、世界模型模拟、反事实推理、对抗性自我质检。

报告必须包含：
1. 谱系与源流（重点）：ReAct（2022）→ Tree of Thoughts（2023）→ Graph of Thoughts（2023）→ LLM+MCTS 的演进——每个的核心数据结构（链/树/图/统计树）、原论文任务、相对前作解决了什么；用传统算法类比（DFS/BFS 剪枝、DAG 工作流、博弈树搜索）；
2. 各方法的适用边界与成本模型（重点）：分支数 × 深度的 token 成本爆炸、什么任务值得上搜索（单路径易死胡同 + 有便宜的验证器/模拟器）、什么任务不值得（有真值反馈的线性任务）；2025–2026 年生产环境的实际采纳现状（搜索类为什么罕见，"能 workflow 别 agent"的工程共识）；
3. 世界模型（world-model simulation）：MCTS rollout 为什么需要模拟器、LLM 语境下世界模型的三种形态（规则引擎/另一个 LLM/仿真环境）、dry-run 沙箱与回测引擎作为"工程世界模型"的定位；
4. 验证器信号质量：为什么"多候选 + 确定性验证器打分"（best-of-n + verifier）是 ToT 的最小可用形态，验证器可靠 vs 验证器也是 LLM 时的差别（与 LLM-as-judge 偏差问题的关系）；
5. 反事实推理：规划时的分支比较与复盘时的归因实验（固定其余变量改一个、前后对比），在 agent 评估中的用法；
6. 对抗性自我质检：critic/self-critique 模式、与独立模型红队的区别、constitutional AI 式自我修订的思路；
7. 实验设计建议：针对"单候选 vs 3 候选修复"的对比实验，指标怎么定（修复成功率的定义：修好目标违规且不引入新违规）、成本怎么报（token 计数）、样本量与确定性（温度、种子）注意什么；
8. 必须掌握的技巧与坑：5 个常见错误（如候选间同质化导致白花钱、验证器被 patch 骗过、只报成功率不报成本）；
9. 面试高频问题：推理期搜索方向 5 个高频问题及答题要点（含"什么时候不该用 ToT/MCTS"）。

要求：用 web 搜索核实关键事实（各论文年份与结论、生产采纳讨论）并标注来源 URL；篇幅 3000 字以上；技术名词首次出现给通俗解释，多用传统软件工程类比。
```
