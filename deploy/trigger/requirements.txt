# Exact pins, not compatible-release: the VM installs with `uv sync --locked`,
# so leaving the *public* starter function on moving releases was the one
# unpinned surface left (round-2 red team). Bump deliberately, then redeploy.
functions-framework==3.8.2
google-cloud-compute==1.19.2
