<?xml version="1.0" encoding="utf-8" ?>
<!-- Deliberately empty.

     This file previously carried
     `<allow until="2026-07-23">field-type-change</allow>`, a dev-time override
     for the one-off embedding migration that changed the tensor from 1536 to
     4096 dims when the Day 4a bake-off replaced MiniMax with Qwen3-8B
     (docs/notes/day4-dense-bakeoff.md). That migration completed; the override
     then sat expired and inert.

     Removed rather than renewed, on purpose: Vespa blocks field-type changes
     because they destroy indexed data, and with no migration in flight that
     block is exactly the behaviour this project wants. A future dimension
     change should have to re-open this door consciously — which is what the
     `until` date was for. (Cleaned 2026-07-27.) -->
<validation-overrides>
</validation-overrides>
